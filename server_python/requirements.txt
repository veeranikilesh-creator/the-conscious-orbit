fastapi>=0.100.0
uvicorn[standard]>=0.22.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.6
pydantic>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
# Real Office Open XML .docx generation for the emailed strategy report.
python-docx>=1.1.0
# The AI engine (Gemini) is called over plain REST — no SDK dependency.
# Set GEMINI_API_KEY in .env; without it every AI feature falls back to a
# deterministic heuristic.
